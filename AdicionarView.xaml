<Window x:Class="Teste_Luz_MVVM_2.AdicionarView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Teste_Luz_MVVM_2"
        mc:Ignorable="d"
        Title="Cliente" Height="450" Width="800" x:Name="jnCliente">
    <Grid>
        <TextBox Text="{Binding Cliente.Nome}" HorizontalAlignment="Left" Height="32" Margin="51,87,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="685"/>
        <Button Content="Confirmar" HorizontalAlignment="Left" Margin="661,360,0,0" VerticalAlignment="Top" Width="75" IsDefault="True" Command="{Binding ConfirmarCommand}" CommandParameter="{Binding ElementName=jnCliente}"/>

    </Grid>
</Window>
